server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Logs para debug
    error_log /var/log/nginx/error.log debug;
    access_log /var/log/nginx/access.log;

    # TESTE: Servir tudo diretamente primeiro
    location / {
        try_files $uri $uri/ /index.html;
    }
}
