server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Debug - log tudo
    error_log /var/log/nginx/error.log debug;
    access_log /var/log/nginx/access.log;

    # Servir todos os arquivos diretamente
    location / {
        try_files $uri $uri/ /index.html;
    }
}
